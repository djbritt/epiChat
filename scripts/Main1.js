import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { Socket } from './Socket';

import {
    Logins
}
from './Login';

ReactDOM.render(<Logins />, document.getElementById('top'));

